# AutoBrain（车智云）项目方案 v2

## 一、项目定位与目标

AutoBrain（车智云）是面向二手车经营团队的数据决策系统，目标是把“收哪台车、多少钱收、怎么经营利润最高”从经验判断升级为数据驱动判断。

### 1.1 业务目标（首年）

- 降低误收概率，提升单车毛利稳定性
- 缩短库存周转天数
- 建立可复用的数据资产与决策流程

### 1.2 核心指标（KPI）

- `收车建议采纳率`：团队采纳系统建议的比例
- `评分命中率`：高评分车辆后续盈利达标比例
- `库存周转天数`：从收车到出售的平均天数
- `单车毛利率`：按车型、城市分层统计
- `价格预测误差`：MAPE / MAE

---

## 二、总体架构（分阶段演进）

```text
数据采集(多平台)
      |
      v
原始数据层(ODS)
      |
      v
标准化与清洗(DWD)
      |
      v
评分与决策引擎
      |
      v
输出层(Excel/看板/API)
```

说明：第一期先走 `Excel 输出`，快速让业务团队使用；后续再升级到 Web Dashboard 与自动化策略引擎。

---

## 三、范围定义

### 3.1 首批数据平台

- 懂车帝
- 转转
- 瓜子二手车
- 闲鱼

### 3.2 首个试点车型

- 小鹏 M03

### 3.3 首期城市范围（建议）

- 海口（单城先跑通）

---

## 四、里程碑规划

## M1（第一期，2-4周）：Excel 数据输出 + 评分决策 MVP

目标：用最小成本跑通“采集 -> 清洗 -> 评分 -> Excel 报表”闭环，支持日常收车判断。

### M1 功能范围

- 多平台基础采集（先稳定抓取小鹏 M03）
- 基础清洗标准化（车型/配置/年款/续航/里程统一）
- 规则评分引擎（0-100 分）
- 每日导出 Excel（运营可直接查看）

### M1 Excel 输出结构（建议 4 个 Sheet）

- `raw_data`：原始抓取明细
- `clean_data`：标准化后的结构化数据
- `score_result`：每条车源评分、评级、建议动作
- `daily_summary`：按城市/配置的均价、分位价、推荐数量

### M1 评分规则（可直接落地）

总分 100，默认权重：

- 价格优势（40）
  - 相对同配置同里程段市场中位价折价越大，分越高
- 里程合理性（20）
  - 相对车龄的“年均里程”偏离越小，分越高
- 上牌时间/车龄（15）
  - 车龄越新，分越高
- 车源质量（15）
  - 个人车源、描述完整、图片数量充分加分
- 流动性（10）
  - 同类车源近7天活跃度高加分

评分分档：

- `A（>=80）`：建议优先跟进（可重点收购）
- `B（65-79）`：可谈价后收购
- `C（50-64）`：谨慎，需人工复核
- `D（<50）`：不建议

### M1 验收标准

- 连续 7 天定时输出 Excel 成功率 >= 95%
- 每天至少 100 条有效车源（按可抓取条件动态调整）
- 评分结果可解释（每条记录显示得分拆解）
- 业务可基于报表完成当日收车优先级排序

### M1 交付物

- 数据采集脚本（可定时）
- 清洗与标准化脚本
- 评分脚本
- Excel 导出脚本与模板
- 《评分规则说明文档 v1》

---

## M2（第二期，4-6周）：价格预测与规则升级

目标：从规则评分升级到“规则 + 统计预测”，提高定价准确性。

### M2 重点能力

- 增加价格预测（分城市、配置、里程段）
- 加入去重与重复发布识别
- 增加价格波动与降价轨迹分析
- 输出“建议收车价区间”而非单点值

### M2 验收标准

- 预测价格 MAPE 控制在可接受区间（先定 8%-12% 目标）
- 重复车源识别准确率显著提升
- 建议收车区间可用于业务谈判

---

## M3（第三期，6-8周）：经营收益模型（卖/短租/长租）

目标：解决“收了之后怎么经营更赚钱”。

### M3 重点能力

- 三种经营策略收益对比：直接出售 / 短租后出售 / 长租后出售
- 纳入真实成本：整备、保险、维修、空置率、资金成本
- 输出三情景测算：乐观 / 基准 / 保守

### M3 验收标准

- 每台候选车可输出策略收益对比表
- 给出推荐经营方案及风险提示
- 支持按月复盘策略实际收益偏差

---

## M4（第四期，8-12周）：Web Dashboard 与运营闭环

目标：从 Excel 过渡到在线系统，形成团队协作与复盘机制。

### M4 重点能力

- 可视化看板（价格趋势、评分分布、库存结构、收益预测）
- 用户权限与操作日志
- 人工反馈回流（采纳/未采纳/实际成交）
- 反馈驱动的模型迭代机制

### M4 验收标准

- 关键角色（老板/采购/运营）按权限使用
- 每周自动产出经营复盘报告
- 模型版本可追溯、可回滚

---

## 五、数据字段设计（M1 必备）

### 5.1 原始层字段（raw_data）

- `platform` 平台
- `listing_id` 平台侧唯一ID（无则用URL哈希）
- `title` 标题
- `brand` 品牌
- `model_raw` 原始车型文本
- `config_raw` 原始配置文本
- `register_date_raw` 原始上牌时间
- `mileage_raw` 原始里程
- `price_raw` 原始价格
- `city_raw` 原始城市
- `seller_type_raw` 个人/商家（原始）
- `publish_time_raw` 原始发布时间
- `url` 链接
- `crawl_time` 抓取时间

### 5.2 清洗层字段（clean_data）

- `brand_std` 标准品牌
- `model_std` 标准车型
- `year_std` 标准年款
- `config_std` 标准配置
- `range_km_std` 标准续航
- `register_date` 标准上牌日期
- `mileage_km` 里程（km）
- `price_wan` 价格（万元）
- `city_std` 标准城市
- `seller_type` 个人/商家
- `age_month` 车龄（月）
- `annual_mileage_km` 年均里程
- `is_valid` 是否有效记录

### 5.3 评分层字段（score_result）

- `score_total`
- `score_price`
- `score_mileage`
- `score_age`
- `score_quality`
- `score_liquidity`
- `rating`（A/B/C/D）
- `decision`（优先跟进/可谈价/人工复核/不建议）
- `score_reason`（文本解释）

### 5.4 与现有采集 Excel 模板对齐

基于你提供的模板《二手车信息表.xlsx》，当前表头为：

- `车型`
- `价格`
- `车源地`
- `行驶里程`
- `首次上牌时间`
- `车况（有无重大事故）`
- `平台`

建议映射关系如下：

- `车型` -> `model_raw`（清洗后进入 `model_std` / `config_std` / `year_std`）
- `价格` -> `price_raw`（清洗后进入 `price_wan`）
- `车源地` -> `city_raw`（清洗后进入 `city_std`）
- `行驶里程` -> `mileage_raw`（清洗后进入 `mileage_km`）
- `首次上牌时间` -> `register_date_raw`（清洗后进入 `register_date`）
- `车况（有无重大事故）` -> `condition_raw`（新增原始字段，清洗后用于 `score_quality`）
- `平台` -> `platform`

### 5.5 基于模板建议新增字段（M1 最小可用）

当前模板可支持基础分析，但要稳定做评分决策，建议在采集端补充以下字段：

- `listing_id`：平台唯一ID（无ID时用URL哈希）
- `url`：原帖链接，便于人工复核
- `crawl_time`：抓取时间，支持时序追踪
- `seller_type_raw`：个人/商家（影响评分）
- `publish_time_raw`：发布时间（用于流动性判断）
- `title`：原始标题（用于去重与语义解析）
- `image_count`：图片数量（辅助判断车源质量）

如需保持 Excel 模板简洁，可把以上字段放在隐藏列，或保留在数据库层，不影响业务同事日常查看。

---

## 六、技术方案（务实版）

### 6.1 技术选型结论（Node.js 主栈）

- 运行时与语言：Node.js 20 LTS + TypeScript
- 爬虫：Playwright（首选）
- API 框架：NestJS（M2 开始）
- 数据处理：Node.js + TypeScript（与后续系统统一）
- 数据库：PostgreSQL（建议配合 Prisma）
- 导出：ExcelJS
- 调度：cron / node-cron（M1），后续可升级 BullMQ
- 日志：Pino

### 6.2 框架选择：NestJS vs Express

结论：本项目建议采用 `NestJS` 作为中长期 API 框架。

对比：

- `NestJS` 优势
  - 模块化与依赖注入清晰，适合多模块（采集、评分、报表、权限）协作
  - TypeScript 生态完整，工程规范更统一
  - 后续接入鉴权、审计、队列、定时任务更顺滑
- `Express` 优势
  - 更轻量，上手快，适合临时接口或 PoC
  - 自由度高，但中大型项目需要更多自建规范

采用策略：

- `M1` 以 Worker 脚本为主（可不强制引入 Web 框架）
- `M2` 开始 API 层统一落到 NestJS，降低后续维护成本

### 6.3 爬虫选择：Playwright vs Puppeteer

结论：本项目首选 `Playwright`，保留 Puppeteer 兼容可能。

对比：

- `Playwright` 优势
  - 自动等待与时序控制能力更强，复杂页面更稳定
  - 多浏览器支持（Chromium / Firefox / WebKit）
  - 对 iframe、弹窗、多上下文处理更友好
- `Puppeteer` 优势
  - 历史更久，资料和存量代码更多
  - 轻量场景可快速启动

采用策略：

- 核心平台采集统一用 Playwright
- 可静态抓取页面补充 `axios + cheerio` 以降低资源消耗
- 预留“平台适配器”接口，必要时可替换为 Puppeteer 或第三方数据源

### 6.4 目录建议（M1）

```text
autobrain/
  crawler/
  pipeline/
  scoring/
  export/
  config/
  docs/
```

### 6.5 稳定性与风控

- 采集限频、随机等待、失败重试
- 反爬异常告警（短信/企业微信可后续接）
- 数据质量校验（空值率、异常值、重复率）
- 全流程日志与每日任务状态记录

---

## 七、合规与边界

- 仅采集公开展示信息，不采集敏感个人隐私
- 遵守目标平台服务条款与 robots 边界
- 严格控制访问频率，避免对平台造成压力
- 保留访问与处理日志，便于审计

---

## 八、风险清单与应对

- 平台结构变化导致采集失效
  - 应对：解析器模块化 + 监控告警 + 快速修复SOP
- 同车多平台重复导致统计失真
  - 应对：多特征去重（标题、里程、价格、时间、图片哈希）
- 无真实成交价导致监督信号不足
  - 应对：先用代理指标（下架时长、降价次数），后续接入内部成交数据
- 业务不采纳模型建议
  - 应对：输出可解释得分，保留人工覆盖入口并复盘

---

## 九、版本计划与节奏建议

- `v2.0`（当前）
  - 明确 M1-M4、先 Excel MVP
- `v2.1`（M1结束后）
  - 基于首月数据校准评分权重
- `v2.2`（M2结束后）
  - 引入预测价区间与更细分城市策略
- `v3.0`（M4后）
  - Web 化 + 持续学习闭环

---

## 十、项目命名

- 中文：车智云
- 英文：AutoBrain
- Slogan：让每一辆车都卖在最佳时机
